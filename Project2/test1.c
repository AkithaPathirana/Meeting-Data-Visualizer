#include<stdio.h>
#include<stdlib.h>
#include<string.h>


typedef struct data {

  int no_meetings;//number of meetings taken by a given person
  int participants; //Number of participants for all meetings
} data_t;

int main(){
  int x,y;
  data_t first,second;
  first.no_meetings=10;
  first.participants=20;
  second.no_meetings=30;
  second.participants=22;
  x=no_meetings;
  y=participants;
  printf("%i",first.x);
  return 0;

}
